public class Math_method {
 static void main(String[] args)
    {
        int a=64,b=81;
        System.out.printf("%.2f %d %d ",Math.sqrt(a),Math.max(a,b),Math.min(a,b));
        int ran = (int) (Math.random() * 14);
        System.out.println(ran);
    }
}
